<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zgcenv.elip.auth.dao.UserDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zgcenv.elip.common.entity.User">
        <id column="id" property="id" />
        <result column="loginName" property="loginName" />
        <result column="password" property="password" />
        <result column="salt" property="salt" />
        <result column="displayName" property="displayName" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="token" property="token" />
        <result column="headImg" property="headImg" />
        <result column="status" property="status" />
        <result column="insertTime" property="insertTime" />
        <result column="updateTime" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, loginName, password, salt, displayName, phone, email, token, headImg, status, insertTime, updateTime
    </sql>

</mapper>
