<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zgcenv.elip.auth.dao.MenuDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zgcenv.elip.common.entity.Menu">
        <id column="id" property="id" />
        <result column="parentId" property="parentId" />
        <result column="menuName" property="menuName" />
        <result column="menuUrl" property="menuUrl" />
        <result column="perms" property="perms" />
        <result column="menuOrder" property="menuOrder" />
        <result column="status" property="status" />
        <result column="insertTime" property="insertTime" />
        <result column="updateTime" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, parentId, menuName, menuUrl, perms, menuOrder, status, insertTime, updateTime
    </sql>

    <select id="findUserPermissions" parameterType="String" resultMap="BaseResultMap">
        select distinct m.perms
        from SYS_role r
                 left join SYS_user_role ur on (r.id = ur.roleId)
                 left join SYS_user u on (u.id = ur.userId)
                 left join SYS_role_menu rm on (rm.roleId = r.id)
                 left join SYS_menu m on (m.id = rm.menuId)
        where u.loginName = #{userName}
          and m.perms is not null
          and m.perms &lt;&gt; ''
    </select>
</mapper>
